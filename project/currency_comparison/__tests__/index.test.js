const index = require('../index.js')

describe("Currency Comparison functionality", () => {
    test("rejects non numeric salary entries", () => {
        
    })
    test("rejects invalid currency codes", ()=>{})
    test("returns response to proper input", ()=>{})
})